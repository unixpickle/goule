// Generated by CoffeeScript 1.7.1
(function() {
  var selectTab, servicesSel, tabServices, tabSettings;

  if (window.goule == null) {
    window.goule = {};
  }

  window.goule.headerControls = {};

  servicesSel = true;

  tabServices = null;

  tabSettings = null;

  window.goule.headerControls.show = function(animate) {
    if (animate) {
      $('#header-controls').fadeIn();
    } else {
      $('#header-controls').css({
        display: 'block',
        opacity: '1.0'
      });
    }
    servicesSel = true;
    return selectTab(tabServices, false);
  };

  window.goule.headerControls.hide = function(animate) {
    if (animate) {
      return $('#header-controls').fadeOut();
    } else {
      return $('#header-controls').css({
        display: 'none'
      });
    }
  };

  selectTab = function(tab, animate) {
    var attributes, right, width;
    width = tab.outerWidth();
    right = $(window).outerWidth() - (width + tab.offset().left) - 10;
    if (animate) {
      attributes = {
        'right': right,
        'width': width
      };
      return $('#tab-selector').animate(attributes, 'fast', function() {});
    } else {
      return $('#tab-selector').css({
        'width': width + 'px',
        'right': right + 'px'
      });
    }
  };

  $(function() {
    $('.generate').mouseenter(function(e) {
      var number;
      number = Math.floor((Math.random() * (8192 - 1024)) + 1024);
      return $('.generate .random').html('' + number);
    });
    tabServices = $('#tab-services');
    tabSettings = $('#tab-settings');
    tabServices.click(function() {
      if (servicesSel) {
        return;
      }
      servicesSel = true;
      selectTab(tabServices, true);
      return false;
    });
    return tabSettings.click(function() {
      if (!servicesSel) {
        return;
      }
      servicesSel = false;
      selectTab(tabSettings, true);
      return false;
    });
  });

}).call(this);
